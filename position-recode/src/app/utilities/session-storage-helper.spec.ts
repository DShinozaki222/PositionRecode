import { SessionStorageHelper } from './session-storage-helper';

describe('SessionStorageHelper', () => {
  it('should create an instance', () => {
    expect(new SessionStorageHelper()).toBeTruthy();
  });
});
