import { LocalStorageHelper } from './local-storage-helper';

describe('LocalStorageHelper.Ts', () => {
  it('should create an instance', () => {
    expect(new LocalStorageHelper()).toBeTruthy();
  });
});
